/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.2 newY.glb 
*/

import React from 'react'
import { useGraph } from '@react-three/fiber'
import { useGLTF, useAnimations } from '@react-three/drei'
import { SkeletonUtils } from 'three-stdlib'

export function NewY(props) {
  const group = React.useRef()
  const { scene, animations } = useGLTF('/3dModels/newY.glb')
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene])
  const { nodes, materials } = useGraph(clone)
  const { actions } = useAnimations(animations, group)
  return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <group name="Armature" scale={0.01}>
          <primitive object={nodes.mixamorigHips} />
          <skinnedMesh name="Plane002" geometry={nodes.Plane002.geometry} material={materials['Material.003']} skeleton={nodes.Plane002.skeleton} />
        </group>
      </group>
    </group>
  )
}

useGLTF.preload('/3dModels/newY.glb')
